# ─────────────────────────────────────────────────────────────
# GPU Training Environment (Python 3.11)
# For training the LSTM model on NVIDIA GPU
# ─────────────────────────────────────────────────────────────
#
# Setup:
#   py -3.11 -m venv gpu_env
#   gpu_env\Scripts\activate        (Windows)
#   source gpu_env/bin/activate     (Linux/Mac)
#
#   pip install torch torchvision torchaudio \
#       --index-url https://download.pytorch.org/whl/cu121
#
#   pip install -r requirements-gpu.txt

numpy>=1.24.0
pandas>=2.0.0
astropy>=5.3.0
scipy>=1.11.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
jupyter>=1.0.0
ipykernel>=6.25.0
requests>=2.31.0
fastapi>=0.110.0
uvicorn>=0.29.0

# torch installed separately via CUDA wheel — see setup above